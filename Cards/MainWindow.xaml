<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Cards.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cards"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Cards">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition x:Name="AddPlayerRow" Height="Auto"/>
            <RowDefinition x:Name="CardRow" Height="*"/>
            <RowDefinition x:Name="HitButtonRow" Height="Auto"/>
            <RowDefinition x:Name="StandButtonRow" Height="Auto"/>
            <RowDefinition x:Name="DoubleDownButtonRow" Height="Auto"/>
            <RowDefinition x:Name="SurrenderButtonRow" Height="Auto"/>
            <RowDefinition x:Name="InsuranceButtonRow" Height="Auto"/>
            <RowDefinition x:Name="SplitButtonRow" Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Vertical">
            <Button Content="Add player" Command="{x:Bind AddPlayerCommand}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5,5,5,5"/>
            <Button Content="Start game" Command="{x:Bind StartGameCommand}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5,5,5,5"/>
            <Button Content="Continue" Click="ContinueGame_Click" Visibility="{x:Bind game.Over, Mode=OneWay}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5,5,5,5"/>
        </StackPanel>
        
    </Grid>
</Window>
