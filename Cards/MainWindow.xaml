<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Cards.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cards"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Title="Cards">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <controls:DockPanel LastChildFill="True">
        <StackPanel controls:DockPanel.Dock="Top"  Orientation="Horizontal">
            <Button Content="Blackjack" Click="ShowBlackjack" Visibility="{x:Bind GameButtonsVisibility, Mode=OneWay}"/>
            <Button Content="Back" Click="Back" Visibility="{x:Bind BackButtonVisibility, Mode=OneWay}"/>
            <Button Content="Close" Click="Close"/>
        </StackPanel>
        <Frame x:Name="MainFrame"/>
    </controls:DockPanel>
    
</Window>
